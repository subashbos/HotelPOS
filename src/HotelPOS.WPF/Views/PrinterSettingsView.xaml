<UserControl x:Class="HotelPOS.Views.PrinterSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">


    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource HeaderBrush}" Padding="24" CornerRadius="14">
            <StackPanel>
                <TextBlock Text="Printer Settings" Style="{StaticResource HeroTextBlockStyle}" Foreground="White"/>
                <TextBlock Text="Configure receipt printer and layout defaults." Margin="0,6,0,0" Style="{StaticResource BodySmallTextBlockStyle}" Foreground="White" Opacity="0.8"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="32" CornerRadius="14">
            <StackPanel MaxWidth="500" HorizontalAlignment="Left">
                <TextBlock Text="General Configuration" Style="{StaticResource H2TextBlockStyle}" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,24"/>

                <TextBlock Text="TARGET PRINTER" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="PrinterList" Padding="10,8" Margin="0,0,0,20">
                    <ComboBoxItem Content="Microsoft Print to PDF (Default)" IsSelected="True"/>
                </ComboBox>

                <TextBlock Text="DEFAULT RECEIPT FORMAT" Style="{StaticResource FieldLabel}"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                    <RadioButton x:Name="FormatThermal" Content="Thermal (80mm)" IsChecked="True" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}"/>
                    <RadioButton x:Name="FormatA4" Content="Standard (A4)" Margin="24,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}"/>
                </StackPanel>

                <TextBlock Text="PRINT PREVIEW" Style="{StaticResource FieldLabel}"/>
                <CheckBox x:Name="ShowPreviewCheck" Content="Show preview dialog before printing" IsChecked="True" Margin="0,0,0,32" Foreground="{DynamicResource TextPrimary}"/>

                <Button Content="💾  Save Settings" Style="{StaticResource PrimaryBtn}" Click="Save_Click"/>
                
                <TextBlock x:Name="StatusText" Margin="0,16,0,0" Foreground="{DynamicResource SuccessBrush}" FontSize="13" Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
