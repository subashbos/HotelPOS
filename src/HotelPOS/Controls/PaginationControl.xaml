<UserControl x:Class="HotelPOS.Controls.PaginationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="50" d:DesignWidth="600">

    <UserControl.Resources>
        <Style x:Key="NavBtn" TargetType="Button">
            <Setter Property="Width"           Value="32"/>
            <Setter Property="Height"          Value="32"/>
            <Setter Property="Background"      Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground"      Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize"        Value="14"/>
            <Setter Property="FontWeight"      Value="Bold"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Padding"         Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource SecondaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource AppBackground}"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="16,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Row info -->
            <TextBlock x:Name="InfoText"
                       VerticalAlignment="Center"
                       FontSize="13" Foreground="{DynamicResource TextMuted}"
                       Text="Showing 1–10 of 0 records"/>

            <!-- Center: Page navigation -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="FirstBtn" Content="«" Style="{StaticResource NavBtn}" Click="First_Click" Margin="0,0,4,0" ToolTip="First page"/>
                <Button x:Name="PrevBtn"  Content="‹" Style="{StaticResource NavBtn}" Click="Prev_Click"  Margin="0,0,8,0" ToolTip="Previous page"/>

                <Border Background="{DynamicResource PrimaryBrush}" BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="1" CornerRadius="6" Padding="14,6">
                    <TextBlock x:Name="PageText" FontSize="13" FontWeight="SemiBold" Foreground="White"/>
                </Border>

                <Button x:Name="NextBtn"  Content="›" Style="{StaticResource NavBtn}" Click="Next_Click"  Margin="8,0,4,0" ToolTip="Next page"/>
                <Button x:Name="LastBtn"  Content="»" Style="{StaticResource NavBtn}" Click="Last_Click"  Margin="0,0,0,0" ToolTip="Last page"/>
            </StackPanel>

            <!-- Right: Page size picker -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                <TextBlock Text="Show:" FontSize="13" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="PageSizeCombo" Width="70" Padding="8,5" FontSize="13"
                          SelectionChanged="PageSize_Changed"
                          Background="{DynamicResource AppBackground}" Foreground="{DynamicResource TextPrimary}" BorderBrush="{DynamicResource BorderBrush}">
                    <ComboBoxItem Content="10"  Tag="10"/>
                    <ComboBoxItem Content="25"  Tag="25"/>
                    <ComboBoxItem Content="50"  Tag="50"/>
                    <ComboBoxItem Content="All" Tag="-1"/>
                </ComboBox>
                <TextBlock Text="per page" FontSize="13" Foreground="{DynamicResource TextMuted}" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
