<UserControl x:Class="HotelPOS.Controls.SpinnerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100">
    <Viewbox>
        <Grid Width="100" Height="100">
            <Canvas Width="100" Height="100" RenderTransformOrigin="0.5,0.5">
                <Canvas.RenderTransform>
                    <RotateTransform x:Name="SpinnerRotate" Angle="0" />
                </Canvas.RenderTransform>
                <Canvas.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
                                    Storyboard.TargetName="SpinnerRotate"
                                    Storyboard.TargetProperty="Angle"
                                    From="0" To="360"
                                    Duration="0:0:1.5"
                                    RepeatBehavior="Forever" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Canvas.Triggers>

                <!-- 8 circles making up the spinner -->
                <Ellipse Width="15" Height="15" Fill="{DynamicResource PrimaryBrush}" Canvas.Left="42.5" Canvas.Top="5" Opacity="1.0" />
                <Ellipse Width="15" Height="15" Fill="{DynamicResource PrimaryBrush}" Canvas.Left="69.0" Canvas.Top="15.98" Opacity="0.8" />
                <Ellipse Width="15" Height="15" Fill="{DynamicResource PrimaryBrush}" Canvas.Left="80" Canvas.Top="42.5" Opacity="0.6" />
                <Ellipse Width="15" Height="15" Fill="{DynamicResource PrimaryBrush}" Canvas.Left="69.0" Canvas.Top="69.0" Opacity="0.4" />
                <Ellipse Width="15" Height="15" Fill="{DynamicResource PrimaryBrush}" Canvas.Left="42.5" Canvas.Top="80" Opacity="0.2" />
                <Ellipse Width="15" Height="15" Fill="{DynamicResource PrimaryBrush}" Canvas.Left="15.98" Canvas.Top="69.0" Opacity="0.1" />
                <Ellipse Width="15" Height="15" Fill="{DynamicResource PrimaryBrush}" Canvas.Left="5" Canvas.Top="42.5" Opacity="0.05" />
                <Ellipse Width="15" Height="15" Fill="{DynamicResource PrimaryBrush}" Canvas.Left="15.98" Canvas.Top="15.98" Opacity="0.0" />
            </Canvas>
        </Grid>
    </Viewbox>
</UserControl>
