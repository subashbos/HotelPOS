<UserControl x:Class="HotelPOS.Views.SessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="{DynamicResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Shift Management (Cash Register)" FontSize="22" FontWeight="Bold" Margin="20,20,20,10" Foreground="{DynamicResource TextPrimary}"/>

        <Grid Grid.Row="1" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Active Session Card -->
            <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="12" Padding="20" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Current Status" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                    <!-- Not Open State -->
                    <StackPanel Visibility="{Binding IsSessionOpen, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverted}">
                        <TextBlock Text="No active shift found. Start a new shift to begin billing." TextWrapping="Wrap" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,20"/>
                        <TextBlock Text="Opening Balance (Cash in hand):" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding OpeningBalance, UpdateSourceTrigger=PropertyChanged}" Height="40" VerticalContentAlignment="Center" Padding="10,0" Margin="0,0,0,15"/>
                        <Button Content="Start New Shift" Command="{Binding OpenSessionCommand}" Height="45" Background="{DynamicResource PrimaryBrush}" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>

                    <!-- Open State -->
                    <StackPanel Visibility="{Binding IsSessionOpen, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Border Background="{DynamicResource PrimaryBrush}" Padding="10" CornerRadius="8" Margin="0,0,0,20">
                            <TextBlock Text="SHIFT ACTIVE" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </Border>
                        
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Opened At:" Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock Text="{Binding CurrentSession.OpenedAt, StringFormat=dd-MMM hh:mm tt}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                        </Grid>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Opening Balance:" Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock Text="{Binding CurrentSession.OpeningBalance, StringFormat=Rs. {0:N2}}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                        </Grid>
                        <Grid Margin="0,0,0,20">
                            <TextBlock Text="Expected Cash:" Foreground="{DynamicResource TextMuted}"/>
                            <TextBlock Text="{Binding ExpectedCash, StringFormat=Rs. {0:N2}}" HorizontalAlignment="Right" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}"/>
                        </Grid>

                        <Separator Margin="0,0,0,20"/>
                        
                        <TextBlock Text="Close Shift" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <TextBlock Text="Actual Cash in Drawer:" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding ActualCash, UpdateSourceTrigger=PropertyChanged}" Height="40" VerticalContentAlignment="Center" Padding="10,0" Margin="0,0,0,10"/>
                        <TextBlock Text="Notes:" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Notes}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Padding="5" Margin="0,0,0,15"/>
                        <Button Content="Close Shift &amp; Reconcile" Command="{Binding CloseSessionCommand}" Height="45" Background="{DynamicResource DangerBrush}" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- History -->
            <Border Grid.Column="2" Background="{DynamicResource SurfaceBrush}" CornerRadius="12" Padding="20" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Shift History" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <DataGrid ItemsSource="{Binding SessionHistory}" Style="{StaticResource PremiumGrid}" MaxHeight="500">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="S.NO" Binding="{Binding SNo}" Width="50"/>
                            <DataGridTextColumn Header="Opened" Binding="{Binding OpenedAt, StringFormat=dd/MM hh:mm}" Width="100"/>
                            <DataGridTextColumn Header="Opening" Binding="{Binding OpeningBalance, StringFormat=N2}" Width="80"/>
                            <DataGridTextColumn Header="Closing" Binding="{Binding ClosingBalance, StringFormat=N2}" Width="80"/>
                            <DataGridTextColumn Header="Actual" Binding="{Binding ActualCash, StringFormat=N2}" Width="80"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
