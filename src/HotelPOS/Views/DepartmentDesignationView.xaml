<UserControl x:Class="HotelPOS.Views.DepartmentDesignationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="760" d:DesignWidth="1150">

    <Grid Background="{DynamicResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ══ HEADER BANNER ══ -->
        <Border Grid.Row="0" Background="{DynamicResource HeaderBrush}" Padding="32,20">
            <StackPanel>
                <TextBlock Text="🏢  Departments &amp; Job Designations" Style="{StaticResource HeroTextBlockStyle}" Foreground="White"/>
                <TextBlock Text="Manage enterprise organizational structure, business departments, and employee job roles."
                           Margin="0,4,0,0" Style="{StaticResource BodySmallTextBlockStyle}" Foreground="White" Opacity="0.8"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- ══ LEFT COLUMN: DEPARTMENTS ══ -->
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="180"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="DEPARTMENTS MASTER" Style="{StaticResource FieldLabel}" FontWeight="Bold" Margin="0,0,0,12"/>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Departments}" SelectedItem="{Binding SelectedDepartment}" AutoGenerateColumns="False" 
                              CanUserAddRows="False" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent"
                              BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Margin="0,0,0,16">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Department Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="1.5*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Department Name" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding DeptName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                            <TextBlock Text="Description" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding DeptDescription, UpdateSourceTrigger=PropertyChanged}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,16"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0" Content="✨ New" Command="{Binding NewDepartmentCommand}" Style="{StaticResource SecondaryButtonStyle}" Height="36"/>
                                <Button Grid.Column="2" Content="💾 Save" Command="{Binding SaveDepartmentCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="36"/>
                                <Button Grid.Column="4" Content="🗑️ Delete" Command="{Binding DeleteDepartmentCommand}" Style="{StaticResource DangerButtonStyle}" Height="36"/>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- ══ RIGHT COLUMN: DESIGNATIONS ══ -->
            <Border Grid.Column="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="180"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="JOB DESIGNATIONS MASTER" Style="{StaticResource FieldLabel}" FontWeight="Bold" Margin="0,0,0,12"/>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Designations}" SelectedItem="{Binding SelectedDesignation}" AutoGenerateColumns="False" 
                              CanUserAddRows="False" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="Transparent"
                              BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Margin="0,0,0,16">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Designation Title" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Department" Binding="{Binding Department.Name}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Designation Title" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding DesigTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                            <TextBlock Text="Department" Style="{StaticResource FieldLabel}"/>
                            <ComboBox ItemsSource="{Binding Departments}" DisplayMemberPath="Name" SelectedItem="{Binding DesigDepartment}" Margin="0,0,0,16"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0" Content="✨ New" Command="{Binding NewDesignationCommand}" Style="{StaticResource SecondaryButtonStyle}" Height="36"/>
                                <Button Grid.Column="2" Content="💾 Save" Command="{Binding SaveDesignationCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="36"/>
                                <Button Grid.Column="4" Content="🗑️ Delete" Command="{Binding DeleteDesignationCommand}" Style="{StaticResource DangerButtonStyle}" Height="36"/>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
