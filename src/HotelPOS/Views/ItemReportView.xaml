<UserControl x:Class="HotelPOS.Views.ItemReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ctrl="clr-namespace:HotelPOS.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{DynamicResource AppBackground}">

    <UserControl.Resources>
        <Style x:Key="DateBoxStyle" TargetType="DatePicker">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Filters Header -->
            <RowDefinition Height="*"/>    <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Top Filter Bar -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <StackPanel>
                    <TextBlock Text="Item Sales Report" Style="{StaticResource H2TextBlockStyle}" Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock Text="Analyze individual item sales volume and revenue generation with granular filters" Style="{StaticResource BodySmallTextBlockStyle}" Foreground="{DynamicResource TextMuted}" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Filters Grid -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/> <!-- Date -->
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>    <!-- Search -->
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/> <!-- Category -->
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/> <!-- Actions -->
                    </Grid.ColumnDefinitions>

                    <!-- Date Range -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Date Range" Style="{StaticResource FieldLabel}"/>
                        <Border Background="{DynamicResource AppBackground}" CornerRadius="10" Padding="12,0" Height="38" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <DatePicker x:Name="FilterFrom" Style="{StaticResource DateBoxStyle}" SelectedDateChanged="Filter_Changed" Width="100"/>
                                <TextBlock Text="to" VerticalAlignment="Center" Margin="8,0" Foreground="{DynamicResource TextMuted}" FontSize="12"/>
                                <DatePicker x:Name="FilterTo" Style="{StaticResource DateBoxStyle}" SelectedDateChanged="Filter_Changed" Width="100"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Search Bar -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Search" Style="{StaticResource FieldLabel}"/>
                        <Border Background="{DynamicResource AppBackground}" CornerRadius="10" Padding="12,0" Height="38" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="🔍" VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.5"/>
                                <TextBox x:Name="SearchText" Grid.Column="1" Background="Transparent" BorderThickness="0" 
                                         VerticalContentAlignment="Center" FontSize="13" TextChanged="Filter_Changed"
                                         Tag="Search by Item Name..."/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- Category -->
                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Category" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="ComboCategory" Width="180" Height="38" DisplayMemberPath="Name" SelectedValuePath="Id" SelectionChanged="Filter_Changed"/>
                    </StackPanel>

                    <!-- Refresh Button -->
                    <StackPanel Grid.Column="6" VerticalAlignment="Bottom">
                        <TextBlock Text=" " Style="{StaticResource FieldLabel}"/>
                        <Button Click="Refresh_Click" Style="{StaticResource PrimaryBtn}" Width="38" Height="38" Padding="0">
                            <TextBlock Text="↻" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- Main Content (Grid) -->
        <Border Grid.Row="1" Padding="32,24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid x:Name="ReportGrid" Style="{StaticResource PremiumGrid}" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="S.NO" Binding="{Binding SNo}" Width="60" HeaderStyle="{StaticResource CenterHeader}" ElementStyle="{StaticResource CenterCell}"/>
                        <DataGridTextColumn Header="ITEM NAME" Binding="{Binding ItemName}" Width="*" ElementStyle="{StaticResource LeftCell}"/>
                        <DataGridTextColumn Header="CATEGORY" Binding="{Binding CategoryName}" Width="200" ElementStyle="{StaticResource LeftCell}"/>
                        <DataGridTextColumn Header="PRICE" Binding="{Binding Price, StringFormat=Rs. {0:N2}}" Width="150" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                        <DataGridTextColumn Header="QUANTITY SOLD" Binding="{Binding QuantitySold}" Width="150" HeaderStyle="{StaticResource CenterHeader}" ElementStyle="{StaticResource CenterCell}"/>
                        <DataGridTextColumn Header="TOTAL REVENUE" Binding="{Binding TotalRevenue, StringFormat=Rs. {0:N2}}" Width="200" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Pagination & Summary Footer -->
                <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20,12" Margin="0,16,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Total Quantity Sold:" Foreground="{DynamicResource TextMuted}" FontSize="13"/>
                            <TextBlock x:Name="TotalQtySold" Text="0" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="8,0,24,0"/>
                            
                            <TextBlock Text="Total Revenue:" Foreground="{DynamicResource TextMuted}" FontSize="13"/>
                            <TextBlock x:Name="TotalRevenueSum" Text="Rs. 0.00" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" Margin="8,0,0,0"/>
                        </StackPanel>

                        <ctrl:PaginationControl x:Name="Pager" Grid.Column="1" HorizontalAlignment="Center"/>

                        <Button Grid.Column="2" Click="Export_Click" Style="{StaticResource ActionBtn}" Foreground="{DynamicResource PrimaryBrush}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📥" Margin="0,0,8,0"/>
                                <TextBlock Text="Export to Excel" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
