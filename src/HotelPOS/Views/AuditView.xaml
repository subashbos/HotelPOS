<UserControl x:Class="HotelPOS.Views.AuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Background="{DynamicResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header Banner -->
            <RowDefinition Height="*"/>     <!-- Content -->
        </Grid.RowDefinitions>

        <!-- ══ HEADER BANNER ══ -->
        <Border Grid.Row="0" Background="{DynamicResource HeaderBrush}" Padding="32,20">
            <StackPanel>
                <TextBlock Text="🛡  System Audit Logs" Style="{StaticResource HeroTextBlockStyle}" Foreground="White"/>
                <TextBlock Text="Track every change made to orders, inventory and settings."
                           Margin="0,4,0,0" Style="{StaticResource BodySmallTextBlockStyle}" Foreground="White" Opacity="0.8"/>
            </StackPanel>
        </Border>

        <!-- ══ MAIN CONTENT ══ -->
        <Grid Grid.Row="1" Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- Filters -->
                <RowDefinition Height="*"/>     <!-- Logs Grid -->
            </Grid.RowDefinitions>

            <!-- Filters -->
            <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16,12" Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="FROM DATE" Style="{StaticResource FieldLabel}"/>
                        <DatePicker x:Name="FromDate" Width="150"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="TO DATE" Style="{StaticResource FieldLabel}"/>
                        <DatePicker x:Name="ToDate" Width="150"/>
                    </StackPanel>
                    <Button Content="🔍  Refresh Logs" Click="Refresh_Click"
                            Style="{StaticResource PrimaryBtn}" Padding="24,10" VerticalAlignment="Bottom"/>
                </StackPanel>
            </Border>

            <!-- Logs Grid -->
            <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1" CornerRadius="12">
                <DataGrid x:Name="AuditGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          Style="{StaticResource PremiumGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="S.NO" Binding="{Binding SNo}" Width="60"/>
                        <DataGridTextColumn Header="TIMESTAMP" Binding="{Binding Timestamp, StringFormat='dd MMM yyyy HH:mm:ss'}" Width="160"/>
                        <DataGridTextColumn Header="USER"      Binding="{Binding Username}" Width="100"/>
                        <DataGridTextColumn Header="ACTION"    Binding="{Binding Action}"   Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="ENTITY"    Binding="{Binding EntityName}" Width="120"/>
                        <DataGridTextColumn Header="DETAILS"   Binding="{Binding Details}"    Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
