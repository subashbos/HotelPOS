<UserControl x:Class="HotelPOS.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1200">


    <Grid Margin="24" Background="{DynamicResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <Border Background="{DynamicResource HeaderBrush}" Padding="28,20" CornerRadius="14">
            <StackPanel>
                <TextBlock Text="⚙️  System Settings" Style="{StaticResource HeroTextBlockStyle}" Foreground="White"/>
                <TextBlock Text="Hotel profile, receipt display options, printer configuration and user management."
                           Margin="0,6,0,0" Style="{StaticResource BodySmallTextBlockStyle}" Foreground="White" Opacity="0.8"/>
            </StackPanel>
        </Border>

        <!-- Tab Control -->
        <TabControl Grid.Row="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="20,10"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </TabControl.Resources>

            <!-- ══ TAB 1: Hotel Profile ══ -->
            <TabItem Header="🏨  Hotel Profile">
                <ScrollViewer>
                    <Border Padding="32">
                        <StackPanel MaxWidth="620" HorizontalAlignment="Left">
                            <TextBlock Text="Business Information" Style="{StaticResource H3TextBlockStyle}" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16"/>

                            <TextBlock Text="HOTEL NAME" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="HotelNameBox" Margin="0,0,0,18"/>

                            <TextBlock Text="FULL ADDRESS" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="HotelAddressBox" Margin="0,0,0,18" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>

                            <TextBlock Text="GSTIN (India Compliance)" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="HotelGstBox" Margin="0,0,0,18" MaxLength="15" CharacterCasing="Upper"/>

                            <TextBlock Text="PHONE NUMBER" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="HotelPhoneBox" Margin="0,0,0,18"/>

                            <Button Content="💾  Save Hotel Profile" Style="{StaticResource PrimaryBtn}" Click="SaveHotel_Click" Padding="28,12" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </TabItem>

            <!-- ══ TAB 2: Printer & Display ══ -->
            <TabItem Header="🖨️  Printer &amp; Display">
                <ScrollViewer>
                    <Border Padding="32">
                        <StackPanel MaxWidth="680" HorizontalAlignment="Left">

                            <!-- Printer Hardware -->
                            <TextBlock Text="Hardware Configuration" Style="{StaticResource H3TextBlockStyle}" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16"/>

                            <TextBlock Text="TARGET PRINTER" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="PrinterList" Padding="10,9" FontSize="14" Margin="0,0,0,18"
                                      Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextPrimary}" BorderBrush="{DynamicResource BorderBrush}"/>

                            <TextBlock Text="DEFAULT RECEIPT FORMAT" Style="{StaticResource FieldLabel}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                                <RadioButton x:Name="FormatThermal" Content="🖨  Thermal (80mm)" VerticalAlignment="Center" FontSize="14" GroupName="FmtGroup" Foreground="{DynamicResource TextPrimary}"/>
                                <RadioButton x:Name="FormatA4"      Content="📄  Standard (A4)"  VerticalAlignment="Center" FontSize="14" GroupName="FmtGroup" Margin="24,0,0,0" Foreground="{DynamicResource TextPrimary}"/>
                            </StackPanel>

                            <CheckBox x:Name="ShowPreviewCheck" Content="Show preview dialog before printing" FontSize="14" Margin="0,0,0,32" Foreground="{DynamicResource TextPrimary}"/>

                            <!-- Separator -->
                            <Separator Margin="0,0,0,24" Background="{DynamicResource BorderBrush}"/>

                            <!-- Receipt Display Toggles -->
                            <TextBlock Text="Receipt Display Options" Style="{StaticResource H3TextBlockStyle}" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16"/>
                            <TextBlock Text="Choose which sections appear on the printed bill." Style="{StaticResource BodySmallTextBlockStyle}" Foreground="{DynamicResource TextMuted}" Margin="0,-10,0,20"/>

                            <Border Background="{DynamicResource AppBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,8">
                                <StackPanel>
                                    <CheckBox x:Name="ShowItemsCheck"    Content="Show item lines on bill"          FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="ShowGstCheck"      Content="Show CGST / SGST breakdown"       FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="ShowDiscountCheck" Content="Show discount line on bill"        FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="ShowPhoneCheck"    Content="Show hotel phone on receipt header" FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="ShowFooterCheck"   Content="Show thank-you footer message"    FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="RoundOffCheck"     Content="Enable bill round-off (round total to nearest rupee)" FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="CompositionCheck"  Content="GST Composition Scheme (Hotel pays 5% tax from turnover; customers are not charged tax; bill title becomes 'Bill of Supply')" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource SuccessBrush}"/>
                                </StackPanel>
                            </Border>

                            <Button Content="💾  Save Printer &amp; Display" Style="{StaticResource PrimaryBtn}" Click="SavePrinter_Click" Margin="0,24,0,0" Padding="28,12" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </TabItem>

            <!-- ══ TAB 3: Users ══ -->
            <TabItem Header="👥  Users">
                <Border Padding="24">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="16"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="User Management" Style="{StaticResource H2TextBlockStyle}" Foreground="{DynamicResource TextPrimary}"/>
                        <ContentControl x:Name="UsersHost" Grid.Row="2"/>
                    </Grid>
                </Border>
            </TabItem>

            <!-- ══ TAB 4: Disaster Recovery ══ -->
            <TabItem Header="💾  Disaster Recovery">
                <ScrollViewer>
                    <Border Padding="32">
                        <StackPanel MaxWidth="620" HorizontalAlignment="Left">
                            <TextBlock Text="Automated Backup Configuration" Style="{StaticResource H3TextBlockStyle}" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16"/>
                            
                            <CheckBox x:Name="EnableAutomatedBackupsCheck" Content="Enable automated database backups on app exit" FontSize="14" Margin="0,0,0,18" Foreground="{DynamicResource TextPrimary}"/>

                            <TextBlock Text="OFF-SITE BACKUP PATH (Network Share or Secondary Drive)" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="OffsiteBackupPathBox" Margin="0,0,0,8" Tag="e.g. D:\Backups or \\NetworkShare\Backup"/>
                            <TextBlock Text="Automated backups will also replicate to this folder if configured." Style="{StaticResource BodySmallTextBlockStyle}" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,24"/>

                            <Button Content="💾  Save Backup Configuration" Style="{StaticResource PrimaryBtn}" Click="SaveBackups_Click" Margin="0,0,0,32" Padding="28,12" HorizontalAlignment="Left"/>

                            <!-- Separator -->
                            <Separator Margin="0,0,0,24" Background="{DynamicResource BorderBrush}"/>

                            <TextBlock Text="Manual Actions" Style="{StaticResource H3TextBlockStyle}" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                                <Button Content="🔄  Create Backup Now" Style="{StaticResource PrimaryBtn}" Click="BackupNow_Click" Padding="20,12"/>
                                <Button Content="↩  Restore Database" Style="{StaticResource DangerBtn}" Click="RestoreDb_Click" Padding="20,12" Margin="16,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </TabItem>

            <!-- ══ TAB 5: Security ══ -->
            <TabItem Header="🔒  Security">
                <ScrollViewer>
                    <Border Padding="32">
                        <StackPanel MaxWidth="620" HorizontalAlignment="Left">
                            <TextBlock Text="Session" Style="{StaticResource H3TextBlockStyle}" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16"/>

                            <TextBlock Text="AUTO-LOGOUT AFTER (MINUTES, 0 = DISABLED)" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="IdleTimeoutBox" Margin="0,0,0,24" Width="120" HorizontalAlignment="Left"/>

                            <Separator Margin="0,0,0,24" Background="{DynamicResource BorderBrush}"/>

                            <TextBlock Text="Outgoing Email (used for password-reset codes)" Style="{StaticResource H3TextBlockStyle}" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,16"/>

                            <TextBlock Text="SMTP HOST" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="SmtpHostBox" Margin="0,0,0,18" Tag="e.g. smtp.gmail.com"/>

                            <TextBlock Text="SMTP PORT" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="SmtpPortBox" Margin="0,0,0,18" Width="120" HorizontalAlignment="Left"/>

                            <CheckBox x:Name="SmtpUseSslCheck" Content="Use SSL/TLS" FontSize="14" Margin="0,0,0,18" Foreground="{DynamicResource TextPrimary}"/>

                            <TextBlock Text="SMTP USERNAME" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="SmtpUsernameBox" Margin="0,0,0,18"/>

                            <TextBlock Text="SMTP PASSWORD" Style="{StaticResource FieldLabel}"/>
                            <PasswordBox x:Name="SmtpPasswordBox" Margin="0,0,0,18"/>

                            <TextBlock Text="FROM ADDRESS" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="SmtpFromBox" Margin="0,0,0,24" Tag="e.g. noreply@yourhotel.com"/>

                            <Button Content="💾  Save Security Settings" Style="{StaticResource PrimaryBtn}" Click="SaveSecurity_Click" Padding="28,12" HorizontalAlignment="Left"/>

                            <TextBlock Text="SEND TEST EMAIL TO" Style="{StaticResource FieldLabel}" Margin="0,24,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                <TextBox x:Name="TestEmailToBox" Width="280" Tag="e.g. you@yourhotel.com"/>
                                <Button x:Name="SendTestEmailButton" Content="✉  Send Test Email" Style="{StaticResource SecondaryBtn}" Click="SendTestEmail_Click" Padding="20,12" Margin="12,0,0,0"/>
                            </StackPanel>

                            <Separator Margin="0,32,0,24" Background="{DynamicResource BorderBrush}"/>

                            <TextBlock Text="My Two-Factor Authentication" Style="{StaticResource H3TextBlockStyle}" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>
                            <TextBlock Text="Adds an authenticator-app code requirement to your own login." Style="{StaticResource BodySmallTextBlockStyle}" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,16"/>

                            <TextBlock x:Name="TwoFactorStatusText" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,16"/>

                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="EnableTwoFactorButton" Content="🔐  Enable Two-Factor Authentication" Style="{StaticResource PrimaryBtn}" Click="EnableTwoFactor_Click" Padding="16,10"/>
                                <Button x:Name="DisableTwoFactorButton" Content="Disable Two-Factor Authentication" Style="{StaticResource DangerBtn}" Click="DisableTwoFactor_Click" Padding="16,10"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
