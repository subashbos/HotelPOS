<UserControl x:Class="HotelPOS.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            
            <Setter Property="Margin"     Value="0,0,0,6"/>
        </Style>
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Padding"         Value="12,10"/>
            <Setter Property="FontSize"        Value="14"/>
            <Setter Property="Margin"          Value="0,0,0,18"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background"      Value="{DynamicResource SurfaceBrush}"/>
            
        </Style>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            
            <Setter Property="Margin"     Value="0,0,0,16"/>
        </Style>
        <Style x:Key="SaveBtn" TargetType="Button">
            <Setter Property="Background"      Value="{DynamicResource SuccessBrush}"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="28,12"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="FontSize"        Value="14"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="10"/>
                </Style>
            </Style.Resources>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24" Background="{DynamicResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <Border Background="{DynamicResource HeaderBrush}" Padding="28,20" CornerRadius="14">
            <StackPanel>
                <TextBlock Text="⚙️  System Settings" FontSize="24" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Hotel profile, receipt display options, printer configuration and user management."
                           Margin="0,6,0,0" FontSize="13" Foreground="#BDD5E7"/>
            </StackPanel>
        </Border>

        <!-- Tab Control -->
        <TabControl Grid.Row="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="20,10"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </TabControl.Resources>

            <!-- ══ TAB 1: Hotel Profile ══ -->
            <TabItem Header="🏨  Hotel Profile">
                <ScrollViewer>
                    <Border Padding="32">
                        <StackPanel MaxWidth="620" HorizontalAlignment="Left">
                            <TextBlock Text="Business Information" Style="{StaticResource SectionHeader}"/>

                            <TextBlock Text="HOTEL NAME" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="HotelNameBox" Style="{StaticResource InputBox}"/>

                            <TextBlock Text="FULL ADDRESS" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="HotelAddressBox" Style="{StaticResource InputBox}" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>

                            <TextBlock Text="GSTIN (India Compliance)" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="HotelGstBox" Style="{StaticResource InputBox}" MaxLength="15" CharacterCasing="Upper"/>

                            <TextBlock Text="PHONE NUMBER" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="HotelPhoneBox" Style="{StaticResource InputBox}"/>

                            <Button Content="💾  Save Hotel Profile" Style="{StaticResource SaveBtn}" Click="SaveHotel_Click" Background="{DynamicResource PrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </TabItem>

            <!-- ══ TAB 2: Printer & Display ══ -->
            <TabItem Header="🖨️  Printer &amp; Display">
                <ScrollViewer>
                    <Border Padding="32">
                        <StackPanel MaxWidth="680" HorizontalAlignment="Left">

                            <!-- Printer Hardware -->
                            <TextBlock Text="Hardware Configuration" Style="{StaticResource SectionHeader}"/>

                            <TextBlock Text="TARGET PRINTER" Style="{StaticResource FieldLabel}"/>
                            <ComboBox x:Name="PrinterList" Padding="10,9" FontSize="14" Margin="0,0,0,18"
                                      Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextPrimary}" BorderBrush="{DynamicResource BorderBrush}"/>

                            <TextBlock Text="DEFAULT RECEIPT FORMAT" Style="{StaticResource FieldLabel}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                                <RadioButton x:Name="FormatThermal" Content="🖨  Thermal (80mm)" VerticalAlignment="Center" FontSize="14" GroupName="FmtGroup" Foreground="{DynamicResource TextPrimary}"/>
                                <RadioButton x:Name="FormatA4"      Content="📄  Standard (A4)"  VerticalAlignment="Center" FontSize="14" GroupName="FmtGroup" Margin="24,0,0,0" Foreground="{DynamicResource TextPrimary}"/>
                            </StackPanel>

                            <CheckBox x:Name="ShowPreviewCheck" Content="Show preview dialog before printing" FontSize="14" Margin="0,0,0,32" Foreground="{DynamicResource TextPrimary}"/>

                            <!-- Separator -->
                            <Separator Margin="0,0,0,24" Background="{DynamicResource BorderBrush}"/>

                            <!-- Receipt Display Toggles -->
                            <TextBlock Text="Receipt Display Options" Style="{StaticResource SectionHeader}"/>
                            <TextBlock Text="Choose which sections appear on the printed bill." FontSize="12" Foreground="{DynamicResource TextMuted}" Margin="0,-10,0,20"/>

                            <Border Background="{DynamicResource AppBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,8">
                                <StackPanel>
                                    <CheckBox x:Name="ShowItemsCheck"    Content="Show item lines on bill"          FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="ShowGstCheck"      Content="Show CGST / SGST breakdown"       FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="ShowDiscountCheck" Content="Show discount line on bill"        FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="ShowPhoneCheck"    Content="Show hotel phone on receipt header" FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="ShowFooterCheck"   Content="Show thank-you footer message"    FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="RoundOffCheck"     Content="Enable bill round-off (round total to nearest rupee)" FontSize="14" Margin="0,0,0,14" Foreground="{DynamicResource TextPrimary}"/>
                                    <CheckBox x:Name="CompositionCheck"  Content="GST Composition Scheme (Hotel pays 5% tax from turnover; customers are not charged tax; bill title becomes 'Bill of Supply')" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource SuccessBrush}"/>
                                </StackPanel>
                            </Border>

                            <Button Content="💾  Save Printer &amp; Display" Style="{StaticResource SaveBtn}" Click="SavePrinter_Click" Margin="0,24,0,0"/>
                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </TabItem>

            <!-- ══ TAB 3: Users ══ -->
            <TabItem Header="👥  Users">
                <Border Padding="24">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="16"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="User Management" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                        <ContentControl x:Name="UsersHost" Grid.Row="2"/>
                    </Grid>
                </Border>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
