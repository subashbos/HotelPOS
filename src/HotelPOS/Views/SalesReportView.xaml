<UserControl x:Class="HotelPOS.Views.SalesReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ctrl="clr-namespace:HotelPOS.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{DynamicResource AppBackground}">

    <UserControl.Resources>
        <Style x:Key="DateBoxStyle" TargetType="DatePicker">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Filters Header -->
            <RowDefinition Height="*"/>    <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Top Filter Bar -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <StackPanel>
                    <TextBlock Text="Detailed Sales Report" Style="{StaticResource HeroTextBlockStyle}" Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock Text="Analyze your transaction history with granular filters" Style="{StaticResource BodySmallTextBlockStyle}" Foreground="{DynamicResource TextMuted}" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Filters Grid -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/> <!-- Date -->
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*" MinWidth="180"/>    <!-- Search -->
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/> <!-- Payment Mode -->
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/> <!-- Category -->
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/> <!-- Order Type -->
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/> <!-- Actions -->
                    </Grid.ColumnDefinitions>

                    <!-- Date Range -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Date Range" Style="{StaticResource FieldLabel}"/>
                        <Border Background="{DynamicResource AppBackground}" CornerRadius="10" Padding="12,0" Height="38" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <DatePicker x:Name="FilterFrom" Style="{StaticResource DateBoxStyle}" SelectedDateChanged="Filter_Changed" Width="100"/>
                                <TextBlock Text="to" VerticalAlignment="Center" Margin="8,0" Foreground="{DynamicResource TextMuted}" FontSize="12"/>
                                <DatePicker x:Name="FilterTo" Style="{StaticResource DateBoxStyle}" SelectedDateChanged="Filter_Changed" Width="100"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Search Bar -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Search" Style="{StaticResource FieldLabel}"/>
                        <Border Background="{DynamicResource AppBackground}" CornerRadius="10" Padding="12,0" Height="38" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="🔍" VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.5"/>
                                <TextBox x:Name="SearchText" Grid.Column="1" Background="Transparent" BorderThickness="0" 
                                         VerticalContentAlignment="Center" FontSize="13" TextChanged="Filter_Changed"
                                         Tag="Search by Invoice or Customer..."/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- Payment Mode -->
                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Payment Mode" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="ComboPayment" Width="120" Height="38" SelectionChanged="Filter_Changed">
                            <ComboBoxItem Content="All" IsSelected="True"/>
                            <ComboBoxItem Content="Cash"/>
                            <ComboBoxItem Content="Card"/>
                            <ComboBoxItem Content="UPI"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Category -->
                    <StackPanel Grid.Column="6">
                        <TextBlock Text="Category" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="ComboCategory" Width="150" Height="38" DisplayMemberPath="Name" SelectedValuePath="Id" SelectionChanged="Filter_Changed"/>
                    </StackPanel>

                    <!-- Order Type (Pills) -->
                    <StackPanel Grid.Column="8">
                        <TextBlock Text="Order Type" Style="{StaticResource FieldLabel}"/>
                        <StackPanel Orientation="Horizontal" Height="38" VerticalAlignment="Center">
                            <RadioButton x:Name="TypeAll" Content="All" IsChecked="True" Style="{StaticResource FilterPill}" Click="Filter_Changed" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <RadioButton x:Name="TypeDine" Content="Dine-In" Style="{StaticResource FilterPill}" Click="Filter_Changed" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <RadioButton x:Name="TypeTake" Content="Takeaway" Style="{StaticResource FilterPill}" Click="Filter_Changed" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <RadioButton x:Name="TypeOnline" Content="Online" Style="{StaticResource FilterPill}" Click="Filter_Changed" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Refresh Button -->
                    <StackPanel Grid.Column="10" VerticalAlignment="Bottom">
                        <TextBlock Text=" " Style="{StaticResource FieldLabel}"/>
                        <Button Click="Refresh_Click" Style="{StaticResource PrimaryBtn}" Width="38" Height="38" Padding="0">
                            <TextBlock Text="↻" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- Main Content (Grid) -->
        <Border Grid.Row="1" Padding="32,24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid x:Name="SalesGrid" Style="{StaticResource PremiumGrid}" IsReadOnly="True" ScrollViewer.ScrollChanged="SalesGrid_ScrollChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="S.NO" Binding="{Binding SNo}" Width="50" HeaderStyle="{StaticResource CenterHeader}" ElementStyle="{StaticResource CenterCell}"/>
                        <DataGridTextColumn Header="DATE &amp; TIME" Binding="{Binding CreatedAt, StringFormat=dd MMM yyyy hh:mm tt}" Width="150" ElementStyle="{StaticResource LeftCell}"/>
                        <DataGridTextColumn Header="INVOICE" Binding="{Binding InvoiceNumber}" Width="130" ElementStyle="{StaticResource LeftCell}"/>
                        <DataGridTextColumn Header="CUSTOMER" Binding="{Binding CustomerName}" Width="*" MinWidth="130" ElementStyle="{StaticResource LeftCell}"/>
                        <DataGridTemplateColumn Header="TYPE" Width="85" HeaderStyle="{StaticResource CenterHeader}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#F0F4F8" CornerRadius="6" Padding="8,2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding OrderType}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextMuted}"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="PAYMENT" Width="85" HeaderStyle="{StaticResource CenterHeader}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding PaymentMode}" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryBrush}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="STATUS" Width="100" HeaderStyle="{StaticResource CenterHeader}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="6" Padding="8,2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E2E8F0"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Paid">
                                                        <Setter Property="Background" Value="#D4EDDA"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Partial">
                                                        <Setter Property="Background" Value="#FFF3CD"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Void">
                                                        <Setter Property="Background" Value="#F8D7DA"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Refunded">
                                                        <Setter Property="Background" Value="#CCE5FF"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="PartiallyRefunded">
                                                        <Setter Property="Background" Value="#E8F4F8"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Status}" FontSize="10" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#495057"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Paid">
                                                            <Setter Property="Foreground" Value="#155724"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Partial">
                                                            <Setter Property="Foreground" Value="#856404"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Void">
                                                            <Setter Property="Foreground" Value="#721C24"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Refunded">
                                                            <Setter Property="Foreground" Value="#004085"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="PartiallyRefunded">
                                                            <Setter Property="Foreground" Value="#104F55"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="AMOUNT" Width="110" HeaderStyle="{StaticResource RightHeader}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Total, StringFormat=Rs. {0:N2}}" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,14,0"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="ACTIONS" Width="150" HeaderStyle="{StaticResource CenterHeader}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Button Style="{StaticResource ActionBtn}" Content="✎" ToolTip="Edit Order" Click="EditOrder_Click" Tag="{Binding}"/>
                                        <Button Style="{StaticResource ActionBtn}" Content="🖨" ToolTip="Print Receipt" Margin="6,0,0,0" Foreground="{DynamicResource AccentBrush}" Click="PrintOrder_Click" Tag="{Binding}"/>
                                        <Button Content="🚫" ToolTip="Void Order" Margin="6,0,0,0" Foreground="{DynamicResource DangerBrush}" Click="VoidOrder_Click" Tag="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource ActionBtn}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Void">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <Button Content="↩" ToolTip="Refund/Return Order" Margin="6,0,0,0" Foreground="{DynamicResource SuccessBrush}" Click="RefundOrder_Click" Tag="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource ActionBtn}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Void">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Refunded">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Pagination & Summary Footer -->
                <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20,12" Margin="0,16,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Total Orders:" Foreground="{DynamicResource TextMuted}" FontSize="13"/>
                            <TextBlock x:Name="TotalOrdersCount" Text="0" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="8,0,24,0"/>
                            
                            <TextBlock Text="Total Revenue:" Foreground="{DynamicResource TextMuted}" FontSize="13"/>
                            <TextBlock x:Name="TotalRevenueSum" Text="Rs. 0.00" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" Margin="8,0,0,0"/>
                        </StackPanel>

                        <Button Grid.Column="1" Click="Export_Click" Style="{StaticResource ActionBtn}" Foreground="{DynamicResource PrimaryBrush}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📥" Margin="0,0,8,0"/>
                                <TextBlock Text="Export to Excel" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
