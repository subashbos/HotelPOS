<UserControl x:Class="HotelPOS.Views.GstR1ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="1400"
             Background="{DynamicResource AppBackground}">

    <UserControl.Resources>
        <Style x:Key="DateBoxStyle" TargetType="DatePicker">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="SummaryLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style x:Key="SummaryValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header Banner -->
            <RowDefinition Height="Auto"/> <!-- Filters -->
            <RowDefinition Height="*"/>    <!-- Tabs -->
        </Grid.RowDefinitions>

        <!-- ══ HEADER BANNER ══ -->
        <Border Grid.Row="0" Background="{DynamicResource HeaderBrush}" Padding="32,20">
            <StackPanel>
                <TextBlock Text="🧾  GSTR-1 Report" Style="{StaticResource HeroTextBlockStyle}" Foreground="White"/>
                <TextBlock Text="Outward supplies to registered (B2B) and unregistered (B2C) customers" Style="{StaticResource BodySmallTextBlockStyle}" Foreground="White" Opacity="0.8" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Top Filter Bar -->
        <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/> <!-- Date -->
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/> <!-- Actions -->
                </Grid.ColumnDefinitions>

                <!-- Date Range -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Filing Period" Style="{StaticResource FieldLabel}"/>
                    <Border Background="{DynamicResource AppBackground}" CornerRadius="10" Padding="12,0" Height="38" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <DatePicker x:Name="FilterFrom" Style="{StaticResource DateBoxStyle}" SelectedDateChanged="Filter_Changed" Width="100"/>
                            <TextBlock Text="to" VerticalAlignment="Center" Margin="8,0" Foreground="{DynamicResource TextMuted}" FontSize="12"/>
                            <DatePicker x:Name="FilterTo" Style="{StaticResource DateBoxStyle}" SelectedDateChanged="Filter_Changed" Width="100"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Actions -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Click="Refresh_Click" Style="{StaticResource PrimaryBtn}" Width="38" Height="38" Padding="0" Margin="0,0,10,0">
                        <TextBlock Text="↻" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                    </Button>
                    <Button Click="Export_Click" Style="{StaticResource ActionBtn}" Foreground="{DynamicResource PrimaryBrush}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📥" Margin="0,0,8,0"/>
                            <TextBlock Text="Export Excel" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ══ TABS ══ -->
        <TabControl Grid.Row="2" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="HeaderTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <ContentPresenter Content="{Binding}" Margin="12,6"/>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </TabControl.Resources>

            <!-- ── B2B INVOICE-WISE TAB ── -->
            <TabItem Header="B2B Invoices">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Summary strip -->
                        <RowDefinition Height="*"/>    <!-- DataGrid -->
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,16">
                        <UniformGrid Columns="5">
                            <StackPanel>
                                <TextBlock x:Name="RecipientsBadge" Text="0" Style="{StaticResource SummaryValue}"/>
                                <TextBlock Text="No. of Recipients" Style="{StaticResource SummaryLabel}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock x:Name="InvoicesBadge" Text="0" Style="{StaticResource SummaryValue}"/>
                                <TextBlock Text="No. of Invoices" Style="{StaticResource SummaryLabel}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock x:Name="TaxableValueBadge" Text="Rs. 0.00" Style="{StaticResource SummaryValue}" Foreground="{DynamicResource PrimaryBrush}"/>
                                <TextBlock Text="Total Taxable Value" Style="{StaticResource SummaryLabel}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock x:Name="TaxAmountBadge" Text="Rs. 0.00" Style="{StaticResource SummaryValue}" Foreground="{DynamicResource TaxAccentBrush}"/>
                                <TextBlock Text="Total Tax Amount" Style="{StaticResource SummaryLabel}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock x:Name="TotalValueBadge" Text="Rs. 0.00" Style="{StaticResource SummaryValue}" Foreground="{DynamicResource SuccessBrush}"/>
                                <TextBlock Text="Total Value" Style="{StaticResource SummaryLabel}"/>
                            </StackPanel>
                        </UniformGrid>
                    </Border>

                    <Border Grid.Row="1" Padding="32,24">
                        <DataGrid x:Name="ReportGrid" Style="{StaticResource PremiumGrid}" IsReadOnly="True" ScrollViewer.ScrollChanged="ReportGrid_ScrollChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="S.NO" Binding="{Binding SNo}" Width="50" HeaderStyle="{StaticResource CenterHeader}" ElementStyle="{StaticResource CenterCell}"/>
                                <DataGridTextColumn Header="GSTIN" Binding="{Binding Gstin}" Width="130" ElementStyle="{StaticResource LeftCell}"/>
                                <DataGridTextColumn Header="INVOICE" Binding="{Binding InvoiceNumber}" Width="110" ElementStyle="{StaticResource LeftCell}"/>
                                <DataGridTextColumn Header="DATE" Binding="{Binding Date, StringFormat=dd-MM-yyyy}" Width="100" ElementStyle="{StaticResource LeftCell}"/>
                                <DataGridTextColumn Header="INVOICE VALUE" Binding="{Binding InvoiceValue, StringFormat=Rs. {0:N2}}" Width="130" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                                <DataGridTextColumn Header="POS" Binding="{Binding Pos}" Width="60" HeaderStyle="{StaticResource CenterHeader}" ElementStyle="{StaticResource CenterCell}"/>
                                <DataGridTextColumn Header="REV. CHARGE" Binding="{Binding ReverseCharge}" Width="90" HeaderStyle="{StaticResource CenterHeader}" ElementStyle="{StaticResource CenterCell}"/>
                                <DataGridTextColumn Header="TYPE" Binding="{Binding InvoiceType}" Width="60" HeaderStyle="{StaticResource CenterHeader}" ElementStyle="{StaticResource CenterCell}"/>
                                <DataGridTextColumn Header="CUSTOMER" Binding="{Binding CustomerName}" Width="*" MinWidth="130" ElementStyle="{StaticResource LeftCell}"/>
                                <DataGridTextColumn Header="TAXABLE VALUE" Binding="{Binding TaxableValue, StringFormat=Rs. {0:N2}}" Width="130" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                                <DataGridTextColumn Header="ITEM TOTAL" Binding="{Binding ItemTotal, StringFormat=Rs. {0:N2}}" Width="120" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                                <DataGridTextColumn Header="RATE" Binding="{Binding Rate, StringFormat={}{0}%}" Width="60" HeaderStyle="{StaticResource CenterHeader}" ElementStyle="{StaticResource CenterCell}"/>
                                <DataGridTextColumn Header="IGST" Binding="{Binding Igst, StringFormat=Rs. {0:N2}}" Width="100" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                                <DataGridTextColumn Header="CGST" Binding="{Binding Cgst, StringFormat=Rs. {0:N2}}" Width="100" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                                <DataGridTextColumn Header="SGST" Binding="{Binding Sgst, StringFormat=Rs. {0:N2}}" Width="100" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- ── B2C(SMALL) SUMMARY TAB ── -->
            <TabItem Header="B2C Summary">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Summary strip -->
                        <RowDefinition Height="*"/>    <!-- DataGrid -->
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,16">
                        <UniformGrid Columns="4">
                            <StackPanel>
                                <TextBlock x:Name="B2cInvoicesBadge" Text="0" Style="{StaticResource SummaryValue}"/>
                                <TextBlock Text="No. of Invoices" Style="{StaticResource SummaryLabel}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock x:Name="B2cTaxableValueBadge" Text="Rs. 0.00" Style="{StaticResource SummaryValue}" Foreground="{DynamicResource PrimaryBrush}"/>
                                <TextBlock Text="Total Taxable Value" Style="{StaticResource SummaryLabel}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock x:Name="B2cTaxAmountBadge" Text="Rs. 0.00" Style="{StaticResource SummaryValue}" Foreground="{DynamicResource TaxAccentBrush}"/>
                                <TextBlock Text="Total Tax Amount" Style="{StaticResource SummaryLabel}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock x:Name="B2cTotalValueBadge" Text="Rs. 0.00" Style="{StaticResource SummaryValue}" Foreground="{DynamicResource SuccessBrush}"/>
                                <TextBlock Text="Total Value" Style="{StaticResource SummaryLabel}"/>
                            </StackPanel>
                        </UniformGrid>
                    </Border>

                    <Border Grid.Row="1" Padding="32,24">
                        <DataGrid x:Name="B2cSummaryGrid" Style="{StaticResource PremiumGrid}" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="RATE" Binding="{Binding Rate, StringFormat={}{0}%}" Width="80" HeaderStyle="{StaticResource CenterHeader}" ElementStyle="{StaticResource CenterCell}"/>
                                <DataGridTextColumn Header="NO. OF INVOICES" Binding="{Binding InvoiceCount}" Width="140" HeaderStyle="{StaticResource CenterHeader}" ElementStyle="{StaticResource CenterCell}"/>
                                <DataGridTextColumn Header="TAXABLE VALUE" Binding="{Binding TaxableValue, StringFormat=Rs. {0:N2}}" Width="150" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                                <DataGridTextColumn Header="CGST" Binding="{Binding Cgst, StringFormat=Rs. {0:N2}}" Width="120" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                                <DataGridTextColumn Header="SGST" Binding="{Binding Sgst, StringFormat=Rs. {0:N2}}" Width="120" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                                <DataGridTextColumn Header="IGST" Binding="{Binding Igst, StringFormat=Rs. {0:N2}}" Width="120" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                                <DataGridTextColumn Header="TOTAL TAX" Binding="{Binding TotalTax, StringFormat=Rs. {0:N2}}" Width="130" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                                <DataGridTextColumn Header="TOTAL VALUE" Binding="{Binding TotalValue, StringFormat=Rs. {0:N2}}" Width="*" MinWidth="130" HeaderStyle="{StaticResource RightHeader}" ElementStyle="{StaticResource RightCell}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
