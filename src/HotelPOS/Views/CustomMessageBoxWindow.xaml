<Window x:Class="HotelPOS.Views.CustomMessageBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Height="Auto" MinHeight="250" MaxHeight="600"
        Width="450"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource AppBackground}"
        BorderThickness="1"
        BorderBrush="{DynamicResource BorderBrush}"
        WindowStyle="None"
        AllowsTransparency="True">

    <Border CornerRadius="16" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" 
            Background="{DynamicResource SurfaceBrush}" Margin="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource HeaderBrush}" CornerRadius="15,15,0,0" Padding="24,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding IconEmoji}" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding Title}" Style="{StaticResource H3TextBlockStyle}" Foreground="White" VerticalAlignment="Center" TextWrapping="Wrap"/>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="24,24">
                <TextBlock Text="{Binding Message}" Style="{StaticResource BodyTextBlockStyle}" Foreground="{DynamicResource TextPrimary}" TextWrapping="Wrap"/>
            </ScrollViewer>

            <!-- Footer Buttons -->
            <Border Grid.Row="2" Background="{DynamicResource AppBackground}" CornerRadius="0,0,15,15" Padding="24,16" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Yes" Command="{Binding YesCommand}" Visibility="{Binding YesButtonVisibility}"
                            Style="{StaticResource PrimaryBtn}" Width="100" Margin="10,0,0,0"/>
                    <Button Content="No" Command="{Binding NoCommand}" Visibility="{Binding NoButtonVisibility}"
                            Style="{StaticResource SecondaryBtn}" Width="100" Margin="10,0,0,0"/>
                    <Button Content="OK" Command="{Binding OkCommand}" Visibility="{Binding OkButtonVisibility}"
                            Style="{StaticResource PrimaryBtn}" Width="100" Margin="10,0,0,0"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}" Visibility="{Binding CancelButtonVisibility}"
                            Style="{StaticResource SecondaryBtn}" Width="100" Margin="10,0,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
