<UserControl x:Class="HotelPOS.Views.PrinterSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    <UserControl.Resources>
        <SolidColorBrush x:Key="HeaderBrush" Color="#173F5F"/>
        <Style TargetType="TextBlock" x:Key="H2">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1A2533"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource HeaderBrush}" Padding="24" CornerRadius="14">
            <StackPanel>
                <TextBlock Text="Printer Settings" FontSize="26" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Configure receipt printer and layout defaults." Margin="0,6,0,0" FontSize="13" Foreground="#BDD5E7"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" BorderBrush="#D8DFEA" BorderThickness="1" Padding="32" CornerRadius="14">
            <StackPanel MaxWidth="500" HorizontalAlignment="Left">
                <TextBlock Text="General Configuration" Style="{StaticResource H2}" Margin="0,0,0,24"/>

                <TextBlock Text="TARGET PRINTER" FontSize="11" FontWeight="Bold" Foreground="#5F6B7A" Margin="0,0,0,8"/>
                <ComboBox x:Name="PrinterList" Padding="10,8" Margin="0,0,0,20">
                    <ComboBoxItem Content="Microsoft Print to PDF (Default)" IsSelected="True"/>
                </ComboBox>

                <TextBlock Text="DEFAULT RECEIPT FORMAT" FontSize="11" FontWeight="Bold" Foreground="#5F6B7A" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                    <RadioButton x:Name="FormatThermal" Content="Thermal (80mm)" IsChecked="True" VerticalAlignment="Center"/>
                    <RadioButton x:Name="FormatA4" Content="Standard (A4)" Margin="24,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="PRINT PREVIEW" FontSize="11" FontWeight="Bold" Foreground="#5F6B7A" Margin="0,0,0,8"/>
                <CheckBox x:Name="ShowPreviewCheck" Content="Show preview dialog before printing" IsChecked="True" Margin="0,0,0,32"/>

                <Button Content="💾  Save Settings" Background="#00A896" Foreground="White" Padding="20,14" FontWeight="Bold" Cursor="Hand" BorderThickness="0" Click="Save_Click"/>
                
                <TextBlock x:Name="StatusText" Margin="0,16,0,0" Foreground="#006B57" FontSize="13" Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
