using Xunit;

// Kept in parity with HotelPOS.Tests: not re-analyzed per-file for thread-safety here,
// so left disabled rather than assumed safe. Can be revisited once proven safe.
[assembly: CollectionBehavior(DisableTestParallelization = true)]
