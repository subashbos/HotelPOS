<UserControl x:Class="HotelPOS.Controls.NotificationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400"
             Visibility="Collapsed">
    <UserControl.Resources>
        <Storyboard x:Key="ShowAnim">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,-20" To="0,0,0,0" Duration="0:0:0.3">
                <ThicknessAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
        </Storyboard>
        <Storyboard x:Key="HideAnim">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.3"/>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,0,0,0" To="0,20,0,-20" Duration="0:0:0.3"/>
        </Storyboard>
    </UserControl.Resources>
    
    <Border x:Name="MainBorder" CornerRadius="12" Padding="20,15" Margin="20"
            MaxWidth="450" HorizontalAlignment="Right" VerticalAlignment="Bottom"
            Background="{DynamicResource SurfaceBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.3" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="IconText" FontSize="20" VerticalAlignment="Center" Margin="0,0,15,0"/>
            
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimary}"/>
                <TextBlock x:Name="MsgText" FontSize="13" Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap" Margin="0,2,0,0"/>
            </StackPanel>
            
            <Button Grid.Column="2" Content="✕" Click="Close_Click"
                    Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextMuted}"
                    FontSize="16" Margin="15,0,0,0" Cursor="Hand" VerticalAlignment="Top"/>
        </Grid>
    </Border>
</UserControl>
